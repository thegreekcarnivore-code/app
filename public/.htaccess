RewriteEngine On

# Let built assets and existing files resolve normally.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send SPA routes back to the Vite entrypoint.
RewriteRule ^ index.html [L]
